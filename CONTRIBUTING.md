# SimpMC-Punish 变更与版本规范

本文档适用于所有提交到 `main` 分支的变更，包括代码、配置、构建脚本和文档。

## 仓库中的版本

本仓库包含两个运行组件，它们分别维护版本号：

| 产物 | 影响范围 | 版本唯一来源 |
| --- | --- | --- |
| Paper/Folia 插件 | `src/`、根目录构建和文档 | 根目录 `pom.xml` 的 `project.version` |
| Velocity 插件 | `simpmc-punish-velocity/` | `simpmc-punish-velocity/pom.xml` 的 `project.version` |

插件的 `plugin.yml` 必须使用 `${project.version}`，由 Maven 资源过滤自动写入版本。不要在 `plugin.yml` 中重复维护固定版本号。

## 每次变更都必须更新版本

- 每个准备合并的 PR 或完整变更集必须更新受影响产物的版本号。
- 一个变更集只更新一次版本；开发过程中的临时提交不需要逐个递增。
- 同时修改 Paper 和 Velocity 时，两个产物都必须分别更新版本。
- 只修改文档、CI、构建配置或仓库级文件时，也必须为对应产物递增补丁版本。
- 新版本必须大于仓库中的旧版本，已经发布过的版本号不得重复使用。
- 每次更新版本时必须同步更新 `CHANGELOG.md`；涉及使用方式时，还应同步更新相关 README。

GitHub Actions 会在 PR 中根据变更路径检查版本是否已经提高。

## 版本格式

版本采用语义化版本 `MAJOR.MINOR.PATCH`，例如 `1.4.2`。多个类型的修改位于同一变更集时，采用影响级别最高的递增方式。

### MAJOR：不兼容变更

递增主版本并将次版本、补丁版本归零，例如 `1.4.2` → `2.0.0`。

适用情况包括：

- 删除或改变已有命令、参数或权限的含义。
- 修改配置格式且无法自动兼容旧配置。
- 进行不兼容的数据库结构、API 或运行环境变更。
- 改变现有行为，导致服务器管理员必须人工迁移。

### MINOR：向后兼容的新功能

递增次版本并将补丁版本归零，例如 `1.4.2` → `1.5.0`。

适用情况包括：

- 新增命令、权限、配置项、GUI 或通知能力。
- 新增向后兼容的数据库字段或公开能力。
- 显著扩展现有功能，但原有用法仍然有效。

### PATCH：向后兼容的维护修改

递增补丁版本，例如 `1.4.2` → `1.4.3`。

适用情况包括：

- 修复错误、安全问题或兼容性问题。
- 性能优化、内部重构和日志改进。
- 依赖、构建、CI、文档或元数据更新。
- 不改变对外功能含义的配置默认值调整。

## 预发布版本

需要公开测试时，可以使用 `-alpha.N`、`-beta.N` 或 `-rc.N`，例如 `1.5.0-beta.1`。预发布序号必须递增；正式发布时移除预发布后缀并保留相同的三段版本，例如 `1.5.0-rc.2` → `1.5.0`。

## 变更流程

1. 明确本次变更影响 Paper、Velocity 还是两者。
2. 根据最高影响级别选择 `MAJOR`、`MINOR` 或 `PATCH`。
3. 更新受影响产物 `pom.xml` 中的版本。
4. 在 `CHANGELOG.md` 顶部记录版本、日期和用户可感知的变化。
5. 更新 README、配置示例和命令帮助等相关文档。
6. 运行相应构建：
   - 插件：`mvn -B clean package`
   - Velocity：`mvn -B -f simpmc-punish-velocity/pom.xml clean package`
7. 确认生成的 JAR 文件名和内部版本与 `pom.xml` 一致后再提交 PR。

## 提交前检查清单

- [ ] 变更范围清晰，没有混入无关修改。
- [ ] 受影响产物的版本已按规则递增。
- [ ] `CHANGELOG.md` 已更新。
- [ ] 命令、权限、配置和 README 保持一致。
- [ ] 相关 Maven 构建通过。
- [ ] 没有提交数据库、密钥、构建产物或本地开发文件。
